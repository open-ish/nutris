export enum Paths {
  about = '/about',
  calculate = '/calculate',
  history = '/history',
  home = '/',
  manageDiets = '/manage-diets',
  patients = '/patients',
  newPatients = '/new-patients',
}

export enum Names {
  about = 'About',
  calculate = 'Calculate',
  history = 'History',
  home = 'Home',
  manageDiets = 'ManageDiets',
  patients = 'Patients',
  newPatients = 'NewPatients',
}
